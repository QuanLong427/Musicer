# Frontend 环境变量配置模板（Next.js）
# 后端配置请查看 backend/.env.example

# Python 后端地址（前端 API 代理转发用）
BACKEND_URL=http://localhost:8000

# 音乐目录（前端本地扫描用，需与 backend/.env 一致）
MUSIC_DIR=Documents/bili

# 客户端 API 基础地址（留空 = 同源，Docker 部署时按需设置）
# NEXT_PUBLIC_API_URL=
